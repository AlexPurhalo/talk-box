%h1= @box.name

%ul
  %li= react_component('CommentSection', comments: raw(render(template: 'comments/index.json.jbuilder')))

= link_to 'Delete', box_path, method: :delete
= link_to 'Home', root_path

